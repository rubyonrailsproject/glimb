#maps.transition-enabled
    -   @maps.each do |map|
        .box.panel.panel-default
            =link_to (image_tag map.image.url), map
            .panel-body
                %p= link_to map.title, map
         

    