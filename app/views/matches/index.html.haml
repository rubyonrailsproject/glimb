// %p.date
//    - if admin_signed_in?
//        =link_to "Admin Sign out", destroy_admin_session_path, :method => :delete
//    - else
//        =link_to "Admin Sign In", admin_session_path, :method => :get
//%p.date
//    - if user_signed_in?
//        =link_to "User Sign out", destroy_user_session_path, :method => :delete
//    - else
//        =link_to "User Sign in", user_session_path, :method => :get
//
//
//
//- if admin_signed_in?
//    =link_to "Add a match", new_match_path
-   @matches.each do |match|
    %h2= link_to match.title, match